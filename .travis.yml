language: php

php:
    - 7.0
    - 5.6
    - 5.5
    - 5.4
    - hhvm

services:
  - redis-server
  - memcached

before_script:
    - composer install
    - mysql -e 'create database mydb;'

script:
    - mkdir -p build/logs
    - phpunit

after_script:
    - vendor/bin/coveralls